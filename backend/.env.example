# ==============================================
# 🛠 CONFIGURATION DU SERVEUR (Backend)
# ==============================================
# Port du serveur Express (3000 car Vite utilise 5173)
PORT=3000

# Environnement : 'development' ou 'production'
NODE_ENV=development

# ==============================================
# 🗄 BASE DE DONNÉES (MongoDB)
# ==============================================
# Choisissez l'une des deux options ci-dessous (décommentez la bonne ligne) :

# Option A : MongoDB Local
# MONGO_URI=mongodb://localhost:27017/roadsync_db

# Option B : MongoDB Atlas (Cloud)
# Remplacer <username>, <password> et <dbname> par vos infos réelles
# MONGO_URI=mongodb+srv://<username>:<password>@cluster0.example.mongodb.net/roadsync_db

# ==============================================
# 🔐 SÉCURITÉ (JWT)
# ==============================================
# Commande pour générer un secret fort dans le terminal :
# node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"

JWT_SECRET=collez_votre_chaine_hexadecimale_ici
JWT_EXPIRES_IN=7d

# ==============================================
# 🌐 CORS & FRONTEND
# ==============================================
# URL du frontend (Vite utilise généralement 5173)
CLIENT_URL=http://localhost:5173